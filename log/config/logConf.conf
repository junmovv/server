#日志等级  0 DEBUG,1 INFO,2 WARN,3 ERROR,4 FATAL
#开关 on 开启 off 关闭

#是否打印在终端
logTerminalSwitch = on
#日志指定等级输出（终端）
logOutputLevelTerminal = 1,3

#是否写入文件
logFileSwitch = off
#日志指定等级输出（文件）
logOutputLevelFile = 0,1,2,3,4

#日志文件保存路径：
logFilePath = ./logFile
#日志文件最大大小(单位为MB 1024MB=1G)：
logMaxSize = 10
